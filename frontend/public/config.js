// Runtime API configuration for a build that must talk to a backend on another
// origin (e.g. Firebase Hosting + a local API via tunnel).
// Leave empty ("") to call the same origin's /api (local dev via the Vite
// proxy). Swap the deployed value without rebuilding the app.
window.UPNEX_API_BASE = "";